Encountered dependency conflict due to strict npm v10+ peer dependency rules.
Resolved by using `npm install --legacy-peer-deps`, which allows installation with older peer ranges.

This preserves compatibility with Chakra UI 1.x and Framer Motion 5.x used in the template.
